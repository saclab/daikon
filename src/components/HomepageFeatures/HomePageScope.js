import React from 'react'
import styles from "./styles.module.css";
import clsx from "clsx";

const HomePageScope = () => {
  return (

    <div className="container" style={{ paddingTop: "40px", background: "rgba(255,255,255, 0.4)" }}>
      <h1 className="hero__title">Daikon is positioned right at the early drug discovery research phase.</h1>
      <div style={{ margin: "40px" }}>
        <img src="/daikon/img/Landing/Scope.png" />
      </div>
    </div>

  )
}

export default HomePageScope